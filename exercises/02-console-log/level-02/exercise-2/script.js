// TODO: Create variables and print a formatted message
let name = "Tenz";
let score = 67;
// the`${} is called a templete used to make the code cleaner it acts like a , or +
console.log(`${name} scored ${score} points`);
