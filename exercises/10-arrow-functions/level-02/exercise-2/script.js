// TODO: Create arrow function isPositive = (x) => { ... }
// Example:
// const isPositive = (x) => {
//     if (x > 0) return true;
//     else return false;
// };

const isEven = (x) => {
    if (x%2 === 0 ) {
        return true;
    } else {
        return false;
    }
};
console.log(isEven(5)); 
console.log(isEven(4));